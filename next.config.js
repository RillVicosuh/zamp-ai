/** @type {import('next').NextConfig} */
const path = require('path');

const nextConfig = {
    images: {
        domains: [
            "res.cloudinary.com"
        ]
    }
}

module.exports = nextConfig
